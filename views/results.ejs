<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Search Results</title>
  <link rel="stylesheet" href="/styles/style.css">
</head>
<body>
  <div class="container">
    <h1>Results</h1>
    <% if (repos && repos.length > 0) { %>
      <% repos.forEach(repo => { %>
        <div class="card-item">
          <h2><a href="<%= repo.html_url %>" target="_blank"><%= repo.full_name %></a></h2>
          <div>

          </div>
          <p><%= repo.description %></p>
          <img src="<%= repo.owner.avatar_url %>" alt="Owner Avatar" height="40px" width="40px" />
          <p>⭐ Stars: <%= repo.stargazers_count %> | 🍴 Forks: <%= repo.forks_count %></p>
          <p> Watching: <%= repo.watchers_count %> | Score: <%= repo.score %></p>
          <p> Language: <%= repo.language %> | Size (KB): <%= repo.size %></p>
        </div>
      <% }) %>
    <% } else { %>
      <p>No repositories found. Try a different description.</p>
    <% } %>
  </div>
</body>
</html>
